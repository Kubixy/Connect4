#ifndef UNTITLED_GAME_H
#define UNTITLED_GAME_H

#include "Data.h"

bool fourInLine(Board &, char, int, int);
bool firstDiagonal(Board &, char, int, int);
bool secondDiagonal(Board &, char, int, int);
bool tieResult(Board &);

#endif
